<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EClinic" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/seed</item>
        <item name="colorPrimaryVariant">@color/seed</item>
        <item name="colorOnPrimary">@color/seed</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/seed</item>
        <item name="colorSecondaryVariant">@color/seed</item>
        <item name="colorOnSecondary">@color/seed</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/seed</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Customize your theme here. -->
    </style>
</resources>