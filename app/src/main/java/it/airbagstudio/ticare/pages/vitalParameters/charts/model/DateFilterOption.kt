package it.airbagstudio.ticare.pages.vitalParameters.charts.model

enum class DateFilterOption {
    ALL,           // Dal...al... (tutte le date)
    LAST_15_DAYS,  // Ultimi 15 giorni
    LAST_WEEK      // Ultima settimana
}
